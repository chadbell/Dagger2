<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.techyourchance.dagger2course.MyApplication"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:targetApi="33">
        <activity
            android:theme="@style/Theme.AppCompat"
            android:name="com.techyourchance.dagger2course.screens.questionslist.QuestionsListActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:theme="@style/Theme.AppCompat"
            android:name="com.techyourchance.dagger2course.screens.questiondetails.QuestionDetailsActivity"/>

        <activity
            android:theme="@style/Theme.AppCompat"
            android:name="com.techyourchance.dagger2course.screens.viewmodel.ViewModelActivity"/>
    </application>

</manifest>
